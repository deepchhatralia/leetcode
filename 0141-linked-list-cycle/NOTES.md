flag is used because intially slow will be equal to fast so while loop will not be executed which specifies the condition of slow != fast.
​
So, flag is used so that intitally while loop starts to execute